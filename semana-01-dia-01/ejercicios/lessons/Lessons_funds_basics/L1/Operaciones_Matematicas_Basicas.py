# Suma - para agregar metricas
total_ventas = 1500 + 2000 + 890
print(f"Total de ventas: {total_ventas}")

# Resta - para calcular diferencias
ingresos = 5000
gastos = 3200
ganancias = ingresos - gastos
print(f"Ganancia: {ganancias}")

# Multiplicacion - para escalado
precio_unitario = 25.50
cantidad = 100
total = precio_unitario * cantidad

# Division - para promedios
total_datos = 1000
numero_registros = 4
promedio = total / numero_registros
print(f"Promedio: {promedio}")

# Division entera - para particionamientos de datos
registros = 1000
tamano_batch = 300
numero_batches = registros // tamano_batch
print(f"Batches necesarios: {numero_batches}")

# Modulo - para verificar paridad/ciclos
registros_id = 15
es_par = registros_id % 2 == 0
print(f"es para? {es_par}")

# Potencia - para calculos estadisticos
base = 2
exponente = 10
resultado = base ** exponente
print(f"2^10 = {resultado}")
